#include <iostream>

using namespace std;

int nqueen(int N) {
  auto backtrack = [
}

int main() {
  cin.tie(0)->sync_with_stdio(0);
  cin.tie(0);
  int N;
  cin >> N;

  cout << nqueen(N) << endl;

  return 0;
}